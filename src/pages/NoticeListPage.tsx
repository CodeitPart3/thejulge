import Input from "@/components/Input";

export default function NoticeListPage() {
  return (
    <div>
      NoticeListPage
      <div>
        <Input label="비밀번호" placeholder="입력" postfix={<span>원</span>} />
      </div>
    </div>
  );
}
